# AI Agent 开发规范与行为约束规则 (AGENTS.md)

本文档是 AI 编程助手与开发者在当前工作区操作时的**最高优先级强制执行规则**。

---

## 🚨 核心铁律一：后端代码修改后必须强制重新运行与验证

凡是涉及到**后端代码（Go 源码、配置文件、路由定义、数据迁移、中间件、数据层等）**的新增、修改、重构或修复，**必须在当前会话中立即执行自动化验证与运行测试**，严禁在未经过实机运行验证的情况下将代码交付给用户。

### 1. 后端强制执行工作流 (Mandatory Backend Workflow)

```
[代码修改完成] 
      ⬇
1. 静态检查与全量编译 (go build ./...) 
      ⬇
2. 自动化测试运行 (go test ./...) 
      ⬇
3. 冒烟运行/启动自省验证 (应用启动与迁移检查)
      ⬇
[确认 100% 绿灯后交付用户]
```

- **全量编译检查 (`go build`)**：针对当前修改的代码目录及所有相关联子模块执行 `go build ./...`，通过标准为退出码 `0`，零编译错误。
- **自动化测试运行 (`go test`)**：执行 `go test ./...`，确保修改没有破坏既有业务逻辑。若针对单体仓（Monorepo），必须包含根包（如 `go test ./framework/... ./contrib/... .`）。
- **启动与自省检查**：涉及入口启动（`cmd/app`、`main.go`、`setup.go`）或数据库迁移（`AutoMigrate`、`seed`、`routesync`）的修改，必须确保初始化流程完整走通。
- **缺陷闭环与止损**：发现报错必须立即在当前轮次定位并修复；严禁未验证直接交付。

---

## 🚨 核心铁律二：前端代码修改后的热更新与运行验证规范

凡是涉及到**前端代码（Vue、React、TypeScript/JavaScript、样式、HTML、前端配置等）**的修改，必须按照以下原则处理：

```
                    [前端代码修改完成]
                            │
            ┌───────────────┴───────────────┐
            ▼                               ▼
    【支持并已启动 HMR 热更新】       【未开启热更新 / 需静态构建】
            │                               │
    • 无需重复重启开发服务          • 必须执行构建/语法检查
    • 检查终端无编译/TS报错         • `pnpm build` / `npm run build`
    • 保证热重载正常捕获最新改动    • 确保最终产物呈现最新运行效果
```

### 1. 开发阶段（HMR 热更新场景）
- 若前端开发服务（如 Vite / Webpack / Turbopack Dev Server）已在运行且支持 **HMR 热模块替换**：
  - **不需要**盲目关闭并重新启动前端开发服务；
  - 必须检查终端输出，确保无 Vue SFC 模板错误、TypeScript 语法错误或 ESLint/Prettier 致命中断；
  - 确保修改的文件能被 HMR 机制正常消费并呈现最新界面效果。

### 2. 非热更新场景 / 构建与配置变更场景
- 遇到以下情况，**必须重新构建或重启以确保呈现最新运行效果**：
  - 1) 修改了环境变量（`.env`、`.env.production`、`.env.development` 等）；
  - 2) 修改了构建工具配置（`vite.config.ts`、`vue.config.js`、`tsconfig.json` 等）；
  - 3) 新增或更新了 NPM/PNPM 依赖（`package.json`、`pnpm-lock.yaml`）；
  - 4) 未启动开发服务器的静态页面或生产打包验证场景（必须执行 `pnpm build` 或 `npm run build` 确保 0 报错通过）。

---

### 三、 框架与架构通用规范

1. **Context 传递规范**：
   - 跨层调用（Handler -> Service -> Biz -> Repo）必须严格传递 `ctx context.Context`，在 HTTP Handler 中使用 `c.Context()`。
2. **接口完整性约束**：
   - 凡是实现或包装框架接口（如 `gorp.Router`、`base64Captcha.Store`、`datacontract.Redis` 等），必须严格实现接口声明的全部方法或内嵌接口类型，避免增减方法后编译中断。
3. **高可用与多实例规范**：
   - 避免全局变量和单机内存状态（如验证码、黑名单），生产级场景接入 Redis 契约或提供优雅降级。
   - 配置文件与资源文件禁止硬编码当前相对路径，必须提供内置兜底或配置化路径解析。
