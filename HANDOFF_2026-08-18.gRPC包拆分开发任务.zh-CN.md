# gRPC 包拆分开发任务（零上下文接手，2026-08-18）

> 本任务：完成 gRPC 契约解耦的**最后一步——包级移动**，让纯 HTTP 应用不再链接 grpc-go（~10MB 体积收益）。
> 前置基础层已提交（commit `80d9387`），本任务在其之上继续。
> 你没有任何历史上下文，请先读"0. 必读"再动手。

---

## 0. 必读（动手前按顺序）

| # | 内容 | 获得什么 |
| --- | --- | --- |
| 1 | 本文档 | 任务本身、当前状态、精确改动清单、验证方案、DoD |
| 2 | `CODE_REVIEW_2026-08-17.zh-CN.md` 第 10 节 | 项目已确立的约定（类型化 key、init 自注册、fail-fast 等），避免重破坏 |
| 3 | `CHANGELOG.md` 的 Breaking changes | 契约 API 现状，改动不得破坏已承诺兼容面 |
| 4 | `README.zh-CN.md` | 如何 build/test（根 module + 嵌套 module 各自 `cd`） |

**警告（避免重踩）：**
- `CHANGELOG.md` 未纳入 git（`.git/info/exclude` 本地排除），内容有效，别困惑。
- `.idea/golinter.xml`、`CODE_REVIEW_2026-07-20.zh-CN.md` 是会话前就存在的无关改动，别动、别提交。
- `examples/` 整个目录被 `.gitignore` 忽略（未入库），改动只作本地验证。
- 体积测试务必用 `-ldflags="-s -w"`（plain 89MB 会误导）。

---

## 1. 任务背景与目标

### 1.1 已完成（commit `80d9387`，不要重做）

gRPC 契约解耦基础层已落地并验证（build/vet/test 绿）：
- `framework/contract/transport/grpc.go`：`GRPCConnFactory.Conn()` / `GRPCServerRegistrar.RegisterProto()` / `Server()` 全部改为 `any`（**不再 import grpc**）
- `framework/contract/runtime/runtime.go`：`BuildGRPCServer() any`（去 grpc import）
- `framework/provider/retry/service.go`：`grpc/status` 改为反射提取 code 字符串 `grpcCodeViaReflect`（语义对齐 `status.FromError`），测试 `grpc_classification_test.go` 已锁定
- `framework/provider/host/adapters.go`：`GRPCService` 用 `grpcServable` 接口 + `stopped` 标志（**不再 import grpc**）
- `framework/bootstrap/http_service.go`：`GRPCServer` 字段/方法改 `any`（去 grpc import）
- `framework/provider/rpc/grpc`：实现新契约签名 + 新增 `MustGRPCServer`/`MustGRPCConn` 断言辅助（`assert.go`）

### 1.2 目标

| 场景 | 现状 | 目标 |
| --- | --- | --- |
| 纯 HTTP 单机应用（`gorp.Run(HTTP())`，mono 模式） | ~42MB（stripped），grpc deps=62 | **~32MB，grpc deps=0** |
| 微服务应用（真正用 gRPC） | ~42MB | 不变（grpc 是真实依赖） |

### 1.3 为什么还差

契约/retry/host/http_service 已 grpc-free，但 HTTP-only 构建仍链接 62 个 grpc 包。剩余 3 个 grpc 链接源：

| # | 文件 | 问题 |
| --- | --- | --- |
| 1 | `framework/bootstrap/grpc_service.go` | **与 http_service 同属 `package bootstrap`**——只要它在包里，整个 bootstrap 就链接 grpc |
| 2 | `framework/provider/metadata/middleware/grpc.go` | 与 `http.go` 同属 `package middleware`，HTTP 主线 import 该包被连带 |
| 3 | `framework/provider/tracing/middleware/grpc.go` | 同上 |

---

## 2. 核心难点：import cycle

把 `grpc_service.go` 直接移出 `package bootstrap` 会产生循环依赖：

```
grpc_service.go 移入新包 bootstrap/grpc 后：
  bootstrap/grpc ──imports──▶ bootstrap   （要 buildGRPCProviders / registerSelectedMicroserviceProvidersWithOptionsFunc / FoundationProviders / ORMRuntimeProviders / DefaultCapabilityProviders）
  bootstrap      ──imports──▶ bootstrap/grpc（http_service.go:672 调用 newGRPCHostableFromRPCServer）
   └── 成环
```

**解法：先打破环，再移动。** 把 `newGRPCHostableFromRPCServer` + `rpcServerHostable` 提取到 grpc-free 的宿主包（`framework/provider/host`，已 grpc-free），http_service.go 改用宿主包版本，bootstrap 就不再需要 import grpc 子包。

`rpcServerHostable` 的方法只有 `Name()/Start()/Stop()`（用 `transportcontract.RPCServer`），完全可 grpc-free 化（其 `grpc *grpc.Server` 字段改为 `grpcServable` 接口或仅存 `RPCServer`，见 Step 2）。

---

## 3. 实施步骤（按顺序，每步保持可编译）

### Step 1：metadata/tracing 的 grpc 拦截器移入 grpc 专用子包

**目的**：让 `provider/metadata/middleware` 和 `provider/tracing/middleware` 两个包 grpc-free。

1. 新建 `framework/provider/metadata/middleware/grpc/`（子包），把 `metadata/middleware/grpc.go` 的内容移入（包名 `grpc`）：
   - `UnaryServerInterceptor(propagator)`、`StreamServerInterceptor(propagator)`、`UnaryClientInterceptor(propagator)`、`StreamClientInterceptor(propagator)` + `wrappedServerStream` 类型
2. 新建 `framework/provider/tracing/middleware/grpc/`（子包），把 `tracing/middleware/grpc.go` 移入：
   - `UnaryServerInterceptor(tracer, serviceName)`、`UnaryClientInterceptor(tracer, serviceName)`
3. 删除原 `metadata/middleware/grpc.go`、`tracing/middleware/grpc.go`（父包不再 import grpc）
4. 更新 3 处调用方（改用新子包 import）：
   - `framework/grpc/interceptor/preset.go:28,31,43`（`tracingmw.`/`metadatamw.` → 新子包）
   - `framework/provider/rpc/grpc/client.go:259,264`（`metadatamw.UnaryClientInterceptor` / `tracingmw.UnaryClientInterceptor`）
   - `framework/provider/rpc/grpc/server.go:305,315,316`（`tracingmw.UnaryServerInterceptor` / `metadatamw.UnaryServerInterceptor` / `metadatamw.StreamServerInterceptor`）
5. **破坏性说明**：直接 `import metadatamw` 并调用 `metadatamw.UnaryServerInterceptor` 的 grpc 应用需改为 import 子包——这是预期内的公开 import 路径变化。

**验证**：`go build ./framework/...`；`go test ./framework/provider/metadata/... ./framework/provider/tracing/... ./framework/provider/rpc/grpc/...`

### Step 2：hostable 提取到 grpc-free 宿主包（打破 cycle）

1. 在 `framework/provider/host/adapters.go`（已 grpc-free）新增：
   ```go
   // NewRPCServerHostable 把 transportcontract.RPCServer 适配为 Hostable。
   func NewRPCServerHostable(name string, server transportcontract.RPCServer) *RPCServerHostable
   type RPCServerHostable struct{ name string; server transportcontract.RPCServer }
   func (h *RPCServerHostable) Name() string
   func (h *RPCServerHostable) Start(ctx) error  // 委托 server.Start
   func (h *RPCServerHostable) Stop(ctx) error   // 委托 server.Stop
   ```
   （原 `rpcServerHostable` 的 `grpc *grpc.Server` 字段只被 `StartGRPCServer` 使用，见 Step 3 处理；hostable 本身只需 `RPCServer`）
2. `framework/bootstrap/http_service.go:672` 改用 `host.NewRPCServerHostable("grpc", rpcServer)`（`provider/host` 已在 import 中）
3. `framework/bootstrap/grpc_service.go:238` 的 `newGRPCHostableFromRPCServer` 调用改为同样用宿主包版本（或保留同包函数，但注意 http_service 不再用它）
4. 删除 `framework/bootstrap/grpc_service.go` 里的 `newGRPCHostableFromRPCServer` + `rpcServerHostable`（若 Step 3 移动该文件，则随文件一起处理，见下）

### Step 3：`grpc_service.go` 移入 `framework/bootstrap/grpc` 子包

1. 新建 `framework/bootstrap/grpc/` 子包，把 `grpc_service.go` 移入（包名 `grpc`）：
   - 内容：`GRPCServiceOptions`、`GRPCServiceRuntime`、`NewGRPCServiceRuntime`、`BootGRPCService`、`RunGRPC`、`StartGRPCServer`、`grpcServerFromRegistrar`
   - 该子包 **import `framework/bootstrap`**（用 `buildGRPCProviders`/`registerSelectedMicroserviceProvidersWithOptionsFunc`/`FoundationProviders`/`ORMRuntimeProviders`/`DefaultCapabilityProviders`）——子包 import 父包不构成环（父包不再 import 子包）
   - `buildGRPCProviders` 若只被本文件用，可一并移入子包
2. 更新引用方：
   - `framework/application/types.go:38`：`type GRPCRuntime = bootstrap.GRPCServiceRuntime` → `grpcbootstrap.GRPCServiceRuntime`
   - `framework/application/types.go` 里 `newGRPCRuntimeFunc`/`bootGRPCServiceFunc`（若有）指向新包
   - 根包 `app.go:87`：`type GRPCRuntime = application.GRPCRuntime`（无需改，application 层已更新）；`ErrGRPCServiceRunFailed`/`ErrGRPCRuntimeBuildFailed` 若来自 application 保持
   - `framework/application` 中任何引用 `bootstrap.GRPCServiceRuntime`/`bootstrap.BootGRPCService` 的地方
3. **破坏性说明**：`bootstrap.BootGRPCService`/`bootstrap.NewGRPCServiceRuntime`/`bootstrap.GRPCServiceOptions` 等公开符号的 import 路径变为 `bootstrap/grpc`——grpc 应用需改 import。
4. **`StartGRPCServer`（grpc_service.go:368）当前无调用者**（已核实），随文件移入子包即可；http_service.go 不调用它，不需要额外处理。http_service 使用 grpc 的唯一路径是 `registerGRPCToHost`（http_service.go:612）→ `newGRPCHostableFromRPCServer`（:672），已由 Step 2 改为宿主包版本。

### Step 4：验证 bootstrap 包完全 grpc-free

1. 确认 `framework/bootstrap` 包（不含子包）不再 import `google.golang.org/grpc`：
   ```bash
   grep -rn "google.golang.org/grpc" framework/bootstrap/*.go   # 应无输出
   ```
2. 确认 `framework/provider/metadata/middleware`、`framework/provider/tracing/middleware` 同理 grpc-free
3. 若仍有 grpc 引用，按 Step 3 的原则继续抽取

---

## 4. 验证方案（三层，必须全过）

1. **编译**：`go build ./...`（根 module）
2. **测试**：`go test ./framework/...`；嵌套 module 各自 `cd` 后 `go test ./...`（contrib 下 24 个）
3. **体积复核**（临时探针，测完删除）：
   ```bash
   mkdir -p .sizeprobe && cat > .sizeprobe/main.go <<'EOF'
   package main
   import ("fmt";"os";"github.com/ngq/gorp")
   func main(){ if err := gorp.Run(gorp.HTTP()); err != nil { fmt.Fprintln(os.Stderr, err); os.Exit(1) } }
   EOF
   go build -ldflags="-s -w" -o /tmp/probe.exe ./.sizeprobe
   ls -la /tmp/probe.exe        # 目标 ~32MB
   go list -deps ./.sizeprobe | grep -c '^google.golang.org/grpc'   # 目标 0
   rm -rf .sizeprobe
   ```
4. **docker gRPC 端到端**（可选但强烈建议）：参考 `contrib/registry/servicecomb/internal/scverify` 的编排模式，起一个 gRPC echo 服务，验证 `RegisterProto→RPC 调用` 全链路。

---

## 5. 完成定义（DoD）

- [ ] `go build ./...`、`go vet ./...`、`go test ./framework/...` 全绿
- [ ] 纯 HTTP 探针（stripped）~32MB，`go list -deps` 的 grpc 包计数为 0
- [ ] `framework/bootstrap`、`framework/provider/metadata/middleware`、`framework/provider/tracing/middleware` 三个包不再 import grpc
- [ ] grpc 应用路径（`provider/rpc/grpc`、`bootstrap/grpc` 子包、`framework/grpc/interceptor`）行为不变，docker 端到端可调
- [ ] 无 import cycle（`go list -deps` 无报错）
- [ ] `CHANGELOG.md` 补记 Breaking changes（import 路径变化）

---

## 6. 参考资料

- 前置 commit：`80d9387`（gRPC 契约解耦基础层，本任务在其之上）
- 交接文档：`HANDOFF_2026-08-18.gRPC解耦方案与待办清单.zh-CN.md`（含体积分析、验证模式）
- 体积实测基线：unified=42MB、gin 地板=14.5MB、grpc-go≈10MB、redis+prom+viper≈9.4MB
- 关键文件现状：
  - `framework/bootstrap/grpc_service.go`（要移出，9 处 grpc 引用）
  - `framework/bootstrap/http_service.go:672`（调用 newGRPCHostableFromRPCServer）
  - `framework/provider/metadata/middleware/grpc.go`、`framework/provider/tracing/middleware/grpc.go`（要移入子包）
  - `framework/grpc/interceptor/preset.go:28,31,43`、`framework/provider/rpc/grpc/{client,server}.go`（拦截器调用方）
  - `framework/application/types.go:38`（`GRPCRuntime = bootstrap.GRPCServiceRuntime` 别名）
  - `app.go:87`（顶层 GRPCRuntime 别名）
