# AI Agent 开发规范与行为约束规则 (AGENTS.md)

本文档是 AI 编程助手与开发者在当前工作区操作时的**最高优先级强制执行规则**。

---

## 🚨 核心铁律：后端代码修改后必须强制重新运行与验证

凡是涉及到**后端代码（Go 源码、配置文件、路由定义、数据迁移、中间件、数据层等）**的新增、修改、重构或修复，**必须在当前会话中立即执行自动化验证与运行测试**，严禁在未经过实机运行验证的情况下将代码交付给用户。

---

### 一、 强制执行工作流 (Mandatory Workflow)

每次修改完后端文件后，必须严格按照以下顺序执行命令行验证：

```
[代码修改完成] 
      ⬇
1. 静态检查与编译 (go build ./...) 
      ⬇
2. 单元测试与覆盖 (go test ./...) 
      ⬇
3. 冒烟运行/启动验证 (应用启动与迁移检查)
      ⬇
[确认无任何错误后交付用户]
```

#### 1. 第一步：全量编译检查 (`go build`)
- 针对当前修改的代码目录及所有相关联子模块，执行：
  ```bash
  go build ./...
  ```
- **通过标准**：退出码为 `0`，零编译错误、零类型不匹配、零未实现接口（如 `gorp.Router`、`context.Context` 等）。

#### 2. 第二步：自动化测试运行 (`go test`)
- 必须运行单元测试以确保修改没有破坏既有业务逻辑：
  ```bash
  go test ./...
  ```
- 若针对框架单体仓（Monorepo），必须包含根包：
  ```bash
  go test ./framework/... ./contrib/... .
  ```
- **通过标准**：所有测试用例 PASS，无任何 FAIL 或 panic。

#### 3. 第三步：启动与运行时自省验证 (Runtime Check)
- 涉及入口启动（`cmd/app`、`main.go`、`setup.go` 等）或数据库迁移（`AutoMigrate`、`seed`、`routesync`）的修改：
  - 必须确保初始化流程完整走通（配置加载、数据库连接、Casbin 模型解析、路由同步等），无相对路径错误或单机状态遗漏。

---

### 二、 缺陷闭环与止损原则

1. **发现报错必须立即在当前轮次修复**：
   - 如果 `go build` 或 `go test` 出现报错，AI Agent 必须根据错误日志立即定位根因并自动修改修复，直到编译和测试全部 100% 绿灯。
2. **严禁主观推测或“纸上谈兵”**：
   - 严禁对用户回答“应该已经修好了”、“理论上可以通过”；必须附带真实的命令执行结果（如 `The command exited with code 0`、`ok package 0.42s`）。

---

### 三、 框架与架构通用规范

1. **Context 传递规范**：
   - 跨层调用（Handler -> Service -> Biz -> Repo）必须严格传递 `ctx context.Context`，在 HTTP Handler 中使用 `c.Context()`。
2. **接口完整性约束**：
   - 凡是实现或包装框架接口（如 `gorp.Router`、`base64Captcha.Store`、`datacontract.Redis` 等），必须严格实现接口声明的全部方法或内嵌接口类型，避免增减方法后编译中断。
3. **高可用与多实例规范**：
   - 避免全局变量和单机内存状态（如验证码、黑名单），生产级场景接入 Redis 契约或提供优雅降级。
   - 配置文件与资源文件禁止硬编码当前相对路径，必须提供内置兜底或配置化路径解析。
