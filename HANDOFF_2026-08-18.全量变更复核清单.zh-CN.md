# 全量变更复核清单（2026-08-18，零上下文复核者）

> 本清单用于**复核**（不是新开发）：逐项核对已完成的修复是否正确、是否有遗漏或回归。
> 你没有任何历史上下文，请先按第 0 节读必读材料，再按第 2 节逐项复核。

---

## 0. 必读材料与验证环境

### 0.1 必读
| # | 文档 | 用途 |
| --- | --- | --- |
| 1 | 本文档 | 复核清单本身 |
| 2 | `CODE_REVIEW_2026-08-16.zh-CN.md` 第 10 节 | 第一轮 11 项发现 + 9 项修复落地状态 |
| 3 | `CODE_REVIEW_2026-08-17.zh-CN.md` 第 8、10 节 | 第二轮 81 项发现 + 65 项修复落地状态 + 三层产品定位 |
| 4 | `CHANGELOG.md` 的 Breaking changes + Added/Changed/Fixed | 全量变更摘要 |

### 0.2 验证命令
```bash
# 根 module
go build ./...
go vet ./...
go test ./framework/... ./contrib/...
go test -race ./framework/...

# 嵌套 module（contrib 下 24 个，各自 cd 后）
for d in $(find contrib -name go.mod | xargs -n1 dirname); do (cd $d && go test ./...); done

# 集成测试编译（运行需真实 broker，不实跑）
cd test/integration && go build ./... && go vet ./...
```

### 0.3 环境警告
- `CHANGELOG.md` 未纳入 git（`.git/info/exclude` 本地排除），内容有效。
- `.idea/golinter.xml`、`CODE_REVIEW_2026-07-20.zh-CN.md` 是会话前就存在的无关改动，勿动勿提交。
- `examples/` 整个目录被 `.gitignore` 忽略（未入库），改动仅作本地验证。
- 体积测试用 `-ldflags="-s -w"`。

---

## 1. 变更总览（本会话 16 个 commit，`493f6ea..HEAD`）

| commit | 主题 | 规模 |
| --- | --- | --- |
| `71fdab0` | 安全：HTTP fail-closed、SSH 主机密钥、JWT 校验、类型化 key | 10 文件 |
| `8e95d0a` | HTTP 中间件 + 核心运行时正确性 | 27 文件 |
| `1c4d9d7` | provider：韧性/缓存/数据层/websocket | 26 文件 |
| `9f17aa3` | contrib：MQ/注册中心/配置源 | 18 文件 |
| `fa4bed9` | 测试工具与集成测试基建 | 6 文件 |
| `b7bf66f` | 两轮 review 文档 | 2 文件 |
| `3fd4fd3` | mTLS 接线 + 内置 tracer 真实 span | 8 文件 |
| `756fb40` | LIFE WaitGroup、Redis PoolStats、BBR 上限 + 文档 | 8 文件 |
| `8221417` | docker 验证修复 5 缺陷 + 验证工具 | 9 文件 |
| `a3a745d` | MQ 广播/队列模型文档 + redisverify 配对 | 3 文件 |
| `75f6ece` | capability fail-fast 决策 | 6 文件 |
| `4ceb756` | gRPC provider init 自注册 | 2 文件 |
| `64e4cd5`/`b513a9b`/`2e6918d` | 交接文档 | 3 文件 |
| `80d9387` | gRPC 契约解耦基础层 | 10 文件 |

---

## 2. 逐项复核清单

### 2.1 安全与认证（commit `71fdab0`、`3fd4fd3`）

| 变更 | 涉及文件 | 复核要点（重点查） |
| --- | --- | --- |
| **SEC-04** HTTP serviceauth fail-closed：authenticator 绑定后无凭证必须经 `Authenticate` | `framework/provider/gin/engine.go` | ① 无认证头请求现在是否被拒（原放行）？② 公开端点/健康检查是否被误伤？③ 与 gRPC 路径行为一致性 |
| **SEC-10** SSH 默认主机密钥校验 | `framework/provider/ssh/service.go` | ① 未配置 known_hosts 时是否回退 `~/.ssh/known_hosts`？② 文件不存在时是否 fail（而非静默跳过）？③ `insecure_skip_host_key: true` 才允许跳过 |
| **SEC-11** JWT issuer/audience/exp 校验 | `contrib/serviceauth/token/provider.go` | ① 签发给 A 的 token 重放给 B 是否被拒？② 旧 token（无 audience）是否兼容被拒？③ namespace/environment 比对是否误伤合法跨环境场景 |
| **KEY-01** 字符串 context key → 类型化 | `framework/contract/security/service_auth_context.go` + 6 个使用文件 | ① 是否还有残留的 `"authorization"`/`"x-service-token"`/`"tls_state"` 字符串 key？② 类型化 key 是否被一致使用 |
| **AUTH-01** mTLS 传输层接线 | `framework/provider/gin/engine.go`、`framework/provider/rpc/grpc/interceptor.go`、`contrib/serviceauth/mtls/provider.go` | ① gin 是否注入 `c.Request.TLS`？② gRPC 是否从 `peer.FromContext` 提取？③ `authenticateByCert` 的过期/链校验是否误伤有效证书 |
| **SSH-02** 连接健康探测 + Close | `framework/provider/ssh/{service,provider}.go` | ① keepalive 探测失败是否剔除重拨？② Close 是否注册到容器 closer |

### 2.2 HTTP 中间件（commit `8e95d0a`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| **ETAG-01** 吞响应修复 | `framework/http/middleware/cache.go` | ① 非 2xx/空 body/304 是否都刷回真实 writer？② 304 是否保留 handler 的 headers？③ SSE/流式是否被缓冲破坏 |
| **IDEM-01** 幂等 Release | `framework/http/middleware/idempotency.go` | ① 失败/panic 是否释放占位符（可重试）？② `Release` 契约变更是否影响自定义 Store 实现者？③ 并发竞态（Release vs 新 Reserve） |
| **BL-01** 死代码删除 | `framework/http/middleware/body_limit.go` | 删除 `errors.Is(ctx.Err(), io.EOF)` 后超限行为是否仍正确 |
| **RATE-LEAK-01** 限流器清扫 | `framework/http/middleware/ratelimit.go` | ① 超阈值清扫是否不误删活跃 key？② 无生产调用点（仅测试用）的确认 |
| **TIMEOUT-01** 超时 code 对齐 | `framework/http/middleware/timeout.go` | code=1006 与 HTTP 504 对齐，客户端判断是否受影响 |

### 2.3 核心运行时（commit `8e95d0a`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| **GOR-01** SafeGo nil 容器 | `framework/goroutine/{safe,logger}.go` | ① nil container 时 recover 路径是否不二次 panic？② stderr 兜底 |
| **EVENT-01** 事件 listener panic | `framework/provider/event/local.go` | ① 同步 Publish 单个 listener panic 是否不影响后续 listener？② 错误是否被包装返回 |
| **LOCK-01/02** config 锁 | `framework/provider/config/service.go` | ① Load 错误路径是否释放锁（不全局死锁）？② 远程 I/O 是否移出锁外？③ watcher 回调是否在锁外触发 |
| **LIFE-01** 生命周期 | `framework/lifecycle/manager.go` | ① nil service 是否拒绝？② Start/Stop panic 是否 recover？③ Stop 是否等待并发 Start（WaitGroup）？④ 状态机是否在失败时回滚 |
| **HEALTH-01** degraded 聚合 | `framework/provider/health/provider.go` | 组件 degraded 是否拉低整体状态 |
| **CRON-01/02** 时区 + panic | `framework/provider/cron/provider.go` | ① `cron.timezone` 是否生效？② panic 是否走 recordExecution/指标路径 |
| **BOOT-01/SIG-01** gRPC 主线 | `framework/bootstrap/grpc_service.go` | ① 失败是否 `c.Destroy()` 清理容器？② 信号注册是否在 Start 前 |
| **GIN-ADAPT-01** 适配器竞态 | `framework/provider/gin/adapter.go` | worker.Request 原子槽回写是否消除跨 goroutine 竞态 |
| **GIN-MODE-01** gin mode 全局 | `framework/provider/gin/provider.go` | `sync.Once` + 冲突告警是否合理 |

### 2.4 provider（commit `1c4d9d7`、`756fb40`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| **DLOCK-01/02/03** noop 锁重写 | `framework/provider/dlock/noop/provider.go` | ① LoadOrStore 是否保证互斥？② 等待者交接是否避免级联释放？③ Lock 是否响应 ctx 取消？④ 条目永久保留的内存影响 |
| **BBR-01~05** 负载保护 | `framework/provider/loadshedding/bbr/*` | ① Record 锁内取桶（无竞态）？② `/proc/stat` 采样 + 非 Linux 回退？③ Close 接线？④ window_size 守卫？⑤ 资源上限逐出 |
| **RETRY-02~06** 重试 | `framework/provider/retry/{service,provider}.go` | ① 大小写失配修复？② MaxAttempts 钳位？③ ctx 错误优先于网络错误？④ AppError 不落子串匹配？⑤ `retry.enabled` 生效 |
| **CACHE-01/02** 缓存 | `framework/provider/cache/{redis,memory,provider}.go` | ① MSet 逐 key SET 带 TTL？② memory closer 接线？③ Close 幂等 |
| **OUTBOX-01/02** | `framework/provider/outbox/memory.go` | ① MarkFailed(nil) 不 panic？② 终态消息限量逐出 |
| **METRICS-02** | `framework/provider/redis/{metrics,provider}.go`、`framework/provider/orm/gorm/metrics.go` | ① PoolStats 采集（增量）？② gorm Wait* 增量累加？③ 原连接 Gauge 已删 |
| **WS-* (7 项)** | `framework/provider/websocket/*` | ① 连接 ctx 独立？② room 断开清理？③ 广播 Close？④ ticker 下限？⑤ 集群计数 TTL+Decr？⑥ client ctx 原子化 |
| **RPC-SRV-01/02/03** | `framework/provider/rpc/grpc/server.go` | ① Register 显式报错？② Serve 错误日志+ServeError？③ Stop 后重启 |

### 2.5 contrib（commit `9f17aa3`、`8221417`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| **MQ-10~14** | `contrib/messagequeue/{rocketmq,kafka,rabbitmq,redis}/*` | ① RocketMQ 自死锁删锁？② kafka 独立 ConsumerGroup？③ rabbitmq 匿名队列 exclusive+auto-delete？④ rabbitmq 连接级重连？⑤ redis 毒丸→死信（5 次退避）？⑥ kafka MaxMessageBytes 默认 |
| **REG-01~06** | `contrib/registry/{etcd,zookeeper,eureka,servicecomb}/*` | ① etcd 重注册循环？② zk ephemeral 重建？③ watch 去重局部化？④ eureka instanceId？⑤ eureka 单实例对象解码？⑥ servicecomb 真实 REST 客户端（find 客户端侧匹配/healthCheck interval/按 instanceId 删/真实 heartbeat） |
| **CFG-01/02** | `contrib/configsource/{consul,etcd}/*` | ① consul 类型断言防御？② watcher 缓存清理 + 删除事件 |
| **CB-01** | `contrib/circuitbreaker/sentinel/*` | strategy 配置生效 |
| **DLOCK-10** | `contrib/dlock/redis/provider.go` | PEXPIRE 毫秒 + 下限钳位 |
| **TRC-01** | `contrib/tracing/otel/provider.go` | sampling_rate:0 生效 |

### 2.6 可观测性（commit `3fd4fd3`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| **OBS-01** label 确定序 | `framework/provider/observability/default.go` | label keys/values 单次遍历排序 + collector 缓存（不重复注册 panic） |
| **OBS-TRACE-01** 内置 tracer | `framework/provider/observability/default.go` + `provider.go` | ① StartSpan 生成真实 trace/span ID？② 父级继承？③ traceparent 传播？④ 环形缓冲上限？⑤ SpanFromContext 语义 |

### 2.7 capability fail-fast（commit `75f6ece`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| fail-fast + 打印后端 | `framework/bootstrap/capability_provider_registry.go` | ① 未知后端返回 failingProvider（Register 报错）？② 未配置/显式 noop 静默回退？③ 有效配置打印 `[gorp] capability backend`？④ 6 个测试文件语义更新正确 |
| 注意 | — | **行为变化**：micro 治理未 import 对应 contrib 包现在启动失败（预期，强制正确配置） |

### 2.8 gRPC 契约解耦（commit `4ceb756`、`80d9387`）

| 变更 | 文件 | 复核要点 |
| --- | --- | --- |
| 契约 grpc-free | `framework/contract/transport/grpc.go`、`framework/contract/runtime/runtime.go` | ① 不再 import grpc？② 接口改 any 后调用方（proto-first 应用）断言正确 |
| retry 本地分类 | `framework/provider/retry/service.go` | `grpcCodeViaReflect` 与 `status.FromError` 语义完全一致（测试 `grpc_classification_test.go` 锁定）——**最高风险点，重点复核** |
| host 接口 | `framework/provider/host/adapters.go` | grpcServable 接口 + stopped 标志，`grpc.ErrServerStopped` 替换是否正确 |
| 适配层 | `framework/provider/rpc/grpc/{client,server,assert}.go` | 新契约签名 + MustGRPCServer/MustGRPCConn |
| **未完成** | `framework/bootstrap/grpc_service.go` 等 | grpc-go 仍被链接（62 包）——完整瘦身需包拆分，见 `HANDOFF_2026-08-18.gRPC包拆分开发任务.zh-CN.md`（**这不是 bug，是已规划的待办**） |

### 2.9 docker 验证工具（commit `8221417`）

| 工具 | 位置 | 复核要点 |
| --- | --- | --- |
| scverify | `contrib/registry/servicecomb/internal/scverify` | ServiceCenter 注册/发现/心跳/下线全流程 |
| kverify | `contrib/messagequeue/kafka/internal/kverify` | 同 group 双订阅 + 2 分区 |
| rverify | `contrib/messagequeue/rabbitmq/internal/rverify` | 匿名队列 + broker 重启重连 |
| redisverify | `contrib/messagequeue/redis/internal/redisverify` | 毒丸→死信（Send+Consume 正确配对） |
| zkverify | `contrib/registry/zookeeper/internal/zkverify` | 会话过期 ephemeralOwner 重建 |

---

## 3. 未修改/待办项（复核时确认"确实没做"且"有记录"）

| 项 | 状态 | 复核确认 |
| --- | --- | --- |
| **BBR-02** 非 Linux CPU 采样 | 未做（Linux 已 /proc/stat，非 Linux 代理回退） | 文档/注释是否声明"生产建议 Linux" |
| **REG-06** ServiceCenter 增强（ak/sk/故障转移/watch 长连接） | 未做（基础已验证） | 是否明确记录为待办 |
| **DUP-01/02** ginContext 重复 | 未做（依赖方向重构） | 是否有记录 |
| **二进制瘦身剩余**（grpc 包拆分） | 未做（已出开发文档） | 交接文档是否完整 |
| **CHANGELOG.md 提交** | 未提交（本地 exclude 挡住） | 内容是否已补记 |
| **IDEM-01** 迁移文档 | 接口注释 + CHANGELOG 已记 | 是否有第三方 Store 迁移示例 |
| **WS-ROOM-01** 反向索引 | 已判定不做（有记录） | 勿误报为遗漏 |
| **capability** 未知后端 | 已改 fail-fast（`75f6ece`） | 勿按旧"回退 noop"预期复核 |

---

## 4. 复核输出要求

复核完成后产出：
1. **问题清单**：每个问题给 ID、严重级、文件:行号、说明、建议修复。
2. **确认项**：逐项确认"通过/有疑问"。
3. **特别关注**：gRPC retry 错误分类语义（`grpc_classification_test.go`）是否与 `status.FromError` 完全一致；capability fail-fast 是否破坏合法 mono 模式；HTTP fail-closed 是否误伤健康检查。

---

## 5. 参考资料索引

- 两轮 review 文档（发现→修复落地状态全记录）
- `CHANGELOG.md`（Breaking/Added/Changed/Fixed 摘要）
- 交接文档 3 份：方案与待办清单、包拆分开发任务、本复核清单
- 关键 commit：`71fdab0`~`2e6918d`（本会话全部）
