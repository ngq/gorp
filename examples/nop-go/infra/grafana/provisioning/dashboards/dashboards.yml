# Grafana 仪表板自动导入配置
apiVersion: 1

providers:
  - name: 'nop-go'
    orgId: 1
    folder: 'nop-go'
    folderUid: 'nop-go'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json