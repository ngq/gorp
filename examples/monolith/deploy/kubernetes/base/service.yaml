# Service 配置
apiVersion: v1
kind: Service
metadata:
  name: monolith
  labels:
    app.kubernetes.io/name: monolith
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: monolith
