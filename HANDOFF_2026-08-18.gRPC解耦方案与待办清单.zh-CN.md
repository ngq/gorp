# gRPC 契约解耦方案 与 待办清单交接（2026-08-18）

> 本文档供接手者（其他模型/协作者）实施。包含两部分：
> **A. gRPC 契约解耦主方案**（二进制瘦身，42MB→32MB）
> **B. 全量未做项清单**（避免交接遗漏）

---

## 0. 项目当前状态速览

- 框架：`github.com/ngq/gorp`，Gin 基础，单机→微服务无缝衔接（gin 增强为主、契约为骨架）。
- 两轮深度 code review（`CODE_REVIEW_2026-08-16/17.zh-CN.md`）共 81 项发现，**P0/P1 全部修复**，`go build/vet/test`（framework+contrib 全模块）全绿，`-race` 无竞态。
- 已完成的后续项：AUTH-01（mTLS 接线）、OBS-TRACE-01（内置真实 tracer）、LIFE-01（WaitGroup）、METRICS-02（PoolStats）、BBR-05（资源上限）、capability fail-fast 决策、MQ 模型文档化。
- 已做 docker 真实验证（ServiceCenter/Kafka/RabbitMQ/Redis/ZooKeeper），修复 5 个真实缺陷，验证程序保留在 `contrib/*/internal/*verify`。
- 本会话提交均在 `main` 分支（共 11 个 review/修复 commit + 此前历史）。
- **未提交的无关改动**（非本会话产生，勿误提交）：`.idea/golinter.xml`、`CODE_REVIEW_2026-07-20.zh-CN.md`。

---

## A. 主方案：gRPC 契约解耦（二进制瘦身）

### A.1 目标

| 场景 | 现状（stripped `-s -w`） | 目标 |
| --- | --- | --- |
| 单机/HTTP 应用（`gorp.Run(HTTP())`，mono 模式，**不用 gRPC**） | ~42MB | **~32MB** |
| 微服务应用（真正启用 gRPC RPC） | ~42MB | 不变（grpc 是真实依赖） |

关键原则：**不用 gRPC 的应用不再为 gRPC 的体积买单**；用 gRPC 的应用体积合理保持。

### A.2 为什么 grpc-go 被强制链接（根因）

`contract/transport/grpc.go` 的接口直接引用了具体类型：
```go
import "google.golang.org/grpc"

type GRPCConnFactory interface {
    Conn(ctx context.Context, service string) (*grpc.ClientConn, error)  // ← 具体类型
}
type GRPCServerRegistrar interface {
    RegisterProto(func(server *grpc.Server) error) error                  // ← 具体类型
    Server() *grpc.Server                                                 // ← 具体类型
}
```
任何 import 框架契约的应用（哪怕纯 HTTP）都被强制链接整个 grpc-go（实测 ~10MB）。这是**依赖倒置被破坏**：契约依赖了具体实现。

### A.3 体积构成证据（已实测，stripped）

| 构成 | 体积 | 说明 |
| --- | --- | --- |
| gin 地板 | ~14.5MB | 不可减（框架基础） |
| grpc-go | ~10MB | **本方案目标** |
| redis+prometheus+viper | ~9.4MB | 开箱即用默认，不动 |
| gorm+sqlx+框架本体 | ~8MB | 不动 |
| 合计 | ~42MB | |

### A.4 必须解耦的 5 个"总是链接"点（精确位置）

以下 5 处即使纯 HTTP 应用也被链接（`google.golang.org/grpc` 主包 / `grpc/status`）：

1. **`framework/contract/transport/grpc.go`**（全文仅此一处契约）
   - `Conn() (*grpc.ClientConn, error)`、`RegisterProto(func(*grpc.Server) error)`、`Server() *grpc.Server`
2. **`framework/contract/runtime/runtime.go:47`**
   - `BuildGRPCServer() *grpc.Server`
3. **`framework/bootstrap/http_service.go`**
   - :99 `GRPCServer *grpc.Server` 字段；:116/:140 `GetGRPCServer()/MustGRPCServer()` 返回 `*grpc.Server`
4. **`framework/provider/retry/service.go`**
   - :17-18 import `grpc/codes`、`grpc/status`；:156 `status.FromError(err)` 做重试错误分类（**行为相关，见 A.6**）
5. **`framework/provider/host/adapters.go`**
   - `GRPCService` 封装 `*grpc.Server`（:120/:131），`Serve`/`Stop` 用 `grpc.ErrServerStopped`

**其余 grpc 引用均属"仅 grpc 应用才链接"**（已确认或天然条件），无需处理：
`bootstrap/grpc_service.go`、`framework/grpc/interceptor/*`、`provider/grpc/interceptor.go`（appgrpc）、`provider/metadata/middleware/grpc.go`、`provider/proto/service_gen.go`、`provider/rpc/grpc/*`（provider 已 init 自注册）、`provider/tracing/middleware/grpc.go`、`rpc/governance/grpc_carrier.go`。**注意**：`rpc/governance/chain.go` 是否被 HTTP-only 默认链路引入需实测确认（若引入则也需处理）。

### A.5 改动设计（兼容层策略）

**目标**：契约层完全不 import `google.golang.org/grpc`，但保持 proto-first 用户代码尽量少改。

1. **契约新增 grpc-free 接口**（`contract/transport/grpc.go` 重写，去掉 grpc import）：
   ```go
   // GRPCRegistrar：用 any 收具体 server，由实现方保证类型
   type GRPCRegistrar interface {
       RegisterProto(register func(server any) error) error
       Server() any
   }
   ```
   - `GRPCConnFactory.Conn()` 改为 `Conn(ctx, service) (any, error)`（返回具体 conn，调用方断言）。
   - `contract/runtime` 的 `BuildGRPCServer() any`。
2. **grpc provider 适配层**（`framework/provider/rpc/grpc`）：
   - 新增 `serverAdapter` 包装 `*grpc.Server`，实现 `RegisterProto(func(any) error)`——内部把 `any` 断言回 `*grpc.Server` 再调 proto 注册。
   - 兼容：proto-first 用户回调从 `func(s *grpc.Server) error` 改为 `func(s any) error { return pb.RegisterXxxServer(s.(*grpc.Server), impl) }`——**用户代码需一次类型断言改动**。
   - 或提供 `MustGRPCServer(any) *grpc.Server` 辅助函数，把断言藏起来，用户代码几乎不变。
3. **`bootstrap/http_service.go`**：`GRPCServer` 字段/方法改返回 `any`（保留 `MustGRPCServer()` 语义，内部断言）。
4. **`provider/host/adapters.go`**：`GRPCService` 改持 `any`（或持一个 `Serve(net.Listener) error` 接口），`grpc.ErrServerStopped` 判断移到 grpc provider 内部。
5. **retry 错误分类（最关键、行为相关）**：`grpc/status.FromError` → 框架本地错误分类：
   - 在 `contract/resilience` 定义 `GrpcStatusError`（或复用现有 `AppError`）包装 code，retry 用 `errors.As` 提取 code 比对。
   - **必须保持现有语义**：`RetryableGRPCCodes` 匹配、`Unavailable/DeadlineExceeded/ResourceExhausted/Aborted` 默认可重试、`status.FromError` 对非 grpc 错误的 `ok=false` 行为。补单测锁定。

### A.6 风险点

| 风险 | 等级 | 缓解 |
| --- | --- | --- |
| 契约签名破坏（`RegisterProto` 回调签名变化） | 高 | 适配层 + `MustGRPCServer` 辅助，用户改动收敛到类型断言 |
| retry 错误分类行为漂移 | 高 | 本地分类器逐条对齐现有 grpc/status 语义 + 单测覆盖 |
| proto-first / grpc-demo 示例编译 | 中 | 同步更新示例（它们是兼容性试金石） |
| `rpc/governance` 是否默认链接 grpc | 中 | 实施前先 `go list -deps` 实测确认 |

### A.7 实施步骤（建议顺序）

1. 实测确认 `rpc/governance/chain.go` 是否被 HTTP-only 默认链路引入（`go list -deps` 查 import 链）。
2. 契约层：`contract/transport/grpc.go`、`contract/runtime/runtime.go` 去 grpc import，改 `any`/接口。
3. retry：本地错误分类器替换 `grpc/status`（先补测试锁语义，再改实现）。
4. grpc provider 适配层 + `bootstrap/http_service.go` + `host/adapters.go` 改接口。
5. 更新 `examples/proto-first-demo`、`examples/grpc-demo` 的注册调用。
6. 三层验证（A.8）。
7. 体积复核：`.sizeprobe` 临时探针（`gorp.Run(HTTP())`）stripped 应 ~32MB；grpc 应用体积不变。

### A.8 验证方案（三层）

1. `go build ./...` + `go vet ./...`（编译期兜住契约破坏）
2. `go test ./framework/... ./contrib/...` + 各嵌套 module `go test ./...`（行为兜底）
3. **docker 端到端 gRPC**：起一个 gRPC echo 服务（参考 `contrib/registry/servicecomb/internal/scverify` 的编排模式），验证 RegisterProto→RPC 调用→governance 中间件全链路；同时跑 `-race`。
4. 体积复核（A.7.7）。

### A.9 完成定义（DoD）

- [ ] 纯 HTTP 应用二进制（stripped）42→**~32MB**；grpc 应用体积不劣化
- [ ] `contract/transport/grpc.go`、`contract/runtime/runtime.go` 不再 import grpc
- [ ] retry 错误分类与改造前语义完全一致（单测覆盖 `RetryableGRPCCodes`/默认码/非 grpc 错误）
- [ ] proto-first-demo、grpc-demo 编译通过并端到端可调
- [ ] 全量 build/vet/test + `-race` 绿

---

## B. 全量未做项清单

### B.1 需外部环境/依赖决策

| 项 | 状态 | 需要什么 | 建议 |
| --- | --- | --- | --- |
| **BBR-02：非 Linux CPU 采样** | Linux 已用 `/proc/stat`（`cpu_proc.go`），非 Linux 回退 goroutine 代理（`cpu_fallback.go`） | 引入 gopsutil 统一跨平台，或文档声明"生产建议 Linux" | 低优先；依赖决策 |
| **REG-06：ServiceCenter 增强** | 基础 register/discover/heartbeat/deregister 已实现并 docker 验证 | 真实 ServiceCenter 联调；ak/sk 鉴权、多 ServerURI 故障转移、watch 长连接未覆盖 | 需真实环境 |

### B.2 架构重构（延期）

| 项 | 内容 | 状态 | 建议 |
| --- | --- | --- | --- |
| **DUP-01/02：ginContext 重复** | `ginContext` 结构体 + `cloneContextForContinuation` 在 `framework/http/middleware/bridge_gin.go` 与 `framework/provider/gin/context.go` 两处重复 | 记录已久未做 | 需调整 middleware 对 gin provider 的依赖方向，独立重构 PR |

### B.3 文档/提交类

| 项 | 状态 | 需要什么 |
| --- | --- | --- |
| **CHANGELOG.md 提交** | 内容已补记（Breaking/Added/Changed/Fixed 全量），但被 `.git/info/exclude:19` 本地排除，未纳入 git | 用户决定是否 `git add -f CHANGELOG.md`（本地 exclude 是个人配置，非仓库规则） |
| **IDEM-01 契约变更迁移说明** | `IdempotencyStore` 新增 `Release`（破坏性），接口注释已标注，CHANGELOG 已记录 | 若第三方自定义 Store，需在文档给出迁移示例 |

### B.4 已判定不做（明确搁置）

| 项 | 原因 |
| --- | --- |
| **WS-ROOM-01：conn→room 反向索引** | 当前 `removeConnFromRooms` 全表扫描在合理规模（房间数千以内）可接受；反向索引增加维护面、纯性能优化，性价比低。若未来房间上万 + 高 churn 再评估 |

### B.5 二进制瘦身后续（可选，需产品决策）

gRPC 契约解耦完成后若仍要更小：
- redis/gorm/指标 惰性化（`gorp.Run(HTTP())` 不再默认带，需显式注册）——**牺牲开箱即用**，需产品决策。
- 注：gin + 框架核心的地板约 18-20MB，**压到 20MB 内需放弃 gin**（与"gin 增强版"定位冲突），不现实。

---

## 4. 参考资料

- 两轮 review 文档：`CODE_REVIEW_2026-08-16.zh-CN.md`、`CODE_REVIEW_2026-08-17.zh-CN.md`（第 10 节落地状态）
- 验证程序（可复用编排模式）：`contrib/{registry/servicecomb, messagequeue/{kafka,rabbitmq,redis}, registry/zookeeper}/internal/*verify`
- 体积探针模式：`gorp.Run(gorp.HTTP())` + `go build -ldflags="-s -w"`，已测得 unified=42MB、gin 地板=14.5MB、grpc=10MB、redis+prom+viper=9.4MB
- 本会话 commit：`71fdab0`~`75f6ece`（安全/核心/contrib/测试/文档/集成验证/capability 决策 等 11 个）
