# {{.ProjectName}}

多服务进阶模板（更强独立治理）

## 先看这三件事

- 创建项目：`gorp new --template multi-independent`
- 启动服务：`go run ./services/user/cmd`
- 本地联调：`make deploy-local`

## 说明

- 适合明确需要更强服务独立治理、独立 go.mod 与后续拆仓演进的团队。
- 推荐先使用 `golayout` 或 `multi-flat-wire` 起步，再按治理需求升级到本结构。

## 项目结构

```text
├── services/                 # 各服务完全独立
│   ├── user/                # 用户服务
│   │   ├── cmd/             # 服务入口
│   │   │   └── main.go      # 入口
│   │   ├── internal/        # 服务内部代码
│   │   ├── config/          # 服务配置
│   │   ├── deploy/     # 服务部署配置
│   │   └── go.mod           # 独立 go.mod
│   ├── order/
│   ├── product/
│   └── gateway/
├── shared/                   # 共享代码
│   ├── config/              # 配置结构
│   ├── db/                  # 数据库
│   ├── infrastructure/      # 基础设施（Redis, Consul, Tracing...）
│   └── logger/              # 日志
├── deploy/              # 统一部署编排
└── go.work                   # Go workspace
```

## 特点

- 服务完全独立
- 独立 go.mod
- 独立部署
- 默认入口直接复用 framework/bootstrap
- 共享层只保留跨服务稳定复用内容

## 默认开发主线

- 每个服务默认从 `services/<service>/cmd/main.go` 启动；
- 默认入口直接复用 framework/bootstrap，不要求先理解 provider/container 内部装配；
- 业务主要写在 `internal/service`、`internal/data`、`internal/server`；
- 共享层只保留跨服务稳定复用内容，不承载默认业务起步教学。

## 快速开始

### 1. 初始化 workspace

```bash
go work sync
```

### 2. 启动服务

```bash
# 本地运行（分别启动）
make run-user    # 终端 1
make run-order   # 终端 2
make run-product # 终端 3

# 或直接运行各服务 main 入口
go run ./services/user/cmd
go run ./services/order/cmd
go run ./services/product/cmd

# Docker 部署
make deploy-local
```

### 3. 访问服务

优先先把一个服务跑通，再逐步扩到多服务联调。

## 日志使用建议

- 默认优先使用 `gorp/log` 作为业务日志入口；
- 先把服务跑通，再按业务需要补日志字段。

## 组件与部署提醒

- 默认继续复用项目自己的 `services/<service>/cmd/main.go` 入口
- 部署与外部组件接入按需展开，不把 README 首屏写成基础设施手册
- 共享层只保留跨服务稳定复用内容

## Makefile 命令

| 命令 | 说明 |
|-----|-----|
| `make build` | 构建所有服务 |
| `make deploy-local` | Docker Compose 部署 |
| `make deploy-local-down` | 停止服务 |
| `make test` | 运行测试 |

## 服务拆分

当团队需要独立发布、独立治理或独立仓库时，再按这个结构继续演进。

```bash
# 1. 复制服务目录
cp -r services/user ../user-service-repo

# 2. 复制 shared 模块（或发布为独立模块）
cp -r shared ../user-service-repo/shared

# 3. 更新 go.mod 中的 replace 指向
```