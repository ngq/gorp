# grpc

This directory is reserved for future Proto-first gRPC register / adapter code.

The default template starts from the HTTP mainline first. Add gRPC transport registration here only when `product-service` actually exposes a gRPC contract.
