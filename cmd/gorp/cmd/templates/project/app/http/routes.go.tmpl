package http

import (
	"github.com/gin-gonic/gin"
	"{{.Module}}/app/http/handler"
)

// RegisterRoutes 挂载 starter 默认业务路由。
func RegisterRoutes(engine *gin.Engine) {
	engine.GET("/api/ping", handler.Ping)
}
