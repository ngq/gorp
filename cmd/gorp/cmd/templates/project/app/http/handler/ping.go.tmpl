package handler

import (
	ginprovider "github.com/ngq/gorp/framework/provider/gin"

	"github.com/gin-gonic/gin"
	"{{.Module}}/app/service"
)

// Ping 返回 starter 默认的最小业务探针响应。
func Ping(c *gin.Context) {
	ginprovider.Success(c, gin.H{"message": service.Ping()})
}
