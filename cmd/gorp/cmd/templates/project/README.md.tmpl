# {{.Name}}

由 `gorp new --template base` 生成的**最小业务项目骨架**。

> 定位说明：
> - 这不是默认推荐的单服务业务 starter；
> - 它更适合最小 demo、结构自定义、快速验证框架主链路，或作为高级用户的起点；
> - 如果你只是想按默认路径快速开始业务开发，优先使用裸 `gorp new`。

## 当前 backend

- `{{.Backend}}`
- 设计原则：统一能力，不统一查询语法
{{- if .IsEntBackend }}
- 当前 starter 的 `ent` 路径仍是骨架占位，后续需要你在项目内补 ent provider / repository 实现。
{{- else }}
- 当前 starter 默认按 `gorm` 路径生成，可直接继续扩展业务代码。
{{- end }}

## 当前模板包含什么

这是一套更适合本地联调的 starter，默认包含：

- `cmd/app/main.go`：项目入口
- `app/http/routes.go`：最小 HTTP 路由
- `app/service/ping.go`：最小业务 service 示例
- `config/app.yaml`：基础配置
- `app/service/`、`app/model/`、`app/provider/`：业务扩展目录
- `storage/log/`、`storage/runtime/`：运行期目录占位
- `docs/structure.md`：starter 目录说明
- `.gitignore`：常见忽略规则

## Quick start

```bash
go mod tidy
go run ./cmd/app
```

## 启动后先验证什么

推荐先访问：

- `GET /healthz`
- `GET /api/ping`
- `/swagger/index.html`（取决于配置）

## 推荐先看哪几个文件

1. `cmd/app/main.go`
2. `app/http/routes.go`
3. `app/service/ping.go`
4. `config/app.yaml`
5. `docs/structure.md`

## 关于当前 starter 的本地联调语义

当前内置 starter 优先服务于“安装 `gorp` → `gorp new` → 立即开始开发”的默认路径。

同时，在框架研发阶段，为了方便母仓内联调，生成结果里的 `go.mod` 可能会带有本地 `replace` 语义，用来指向当前框架源码目录。

适合场景：
- 直接通过 `gorp new` 快速创建并启动项目
- 在框架研发过程中生成一个 demo/starter 工程快速联调

如果你明确需要一个更接近“外部用户拿到的固定版本 starter 项目”的结果，请使用：

```bash
gorp template pack
gorp new from-release --repo <owner>/<repo> --tag latest
```
