# {{.Name}} template base

这是 gorp starter 的共享模板基底。

它不是公开推荐的业务 starter，也不承载单服务或多服务的入口叙事。公开模板应从下面三类选择：

- `golayout`：默认单服务 Gin Web API starter
- `multi-flat-wire`：默认微服务 starter
- `multi-independent`：进阶独立治理多服务 starter

## 这个基底包含什么

- `.gorp-template.yml`：模板元信息
- `.gitignore`：共享忽略规则
- `README.md`：基底定位说明
- `docs/structure.md`：共享结构边界
- `docs/deploy.md`：共享交付片段说明
- `docs/next-steps.md`：后续公开模板重写顺序
- `deploy/kubernetes/base/*`：最小 Kubernetes 共享片段
- `deploy/kubernetes/overlays/*`：环境 overlay 示例片段

## 使用边界

- 不在这里放 `cmd/app`、`app/http`、`internal/server/*`
- 不在这里放业务 handler、service、repository
- 不在这里放 `go.mod`、Dockerfile、Makefile、CI 主线
- 不在这里解释 framework 内部 provider / container / bootstrap

这些内容由公开模板按场景重写并拥有。
