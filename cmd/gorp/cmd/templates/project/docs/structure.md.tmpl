# 共享 starter 基底目录说明

当前基底层只保留多模板共享的最小目录：

- `cmd/app/`：共享默认入口骨架
- `app/http/`：最小 HTTP 路由入口
- `app/service/`：最小业务服务示例
- `app/model/`：数据模型预留目录
- `app/provider/`：自定义 provider 预留目录
- `config/`：基础配置
- `storage/`：日志与运行时目录占位

## 推荐扩展顺序

1. 先在公开模板里承接业务起步路径
2. 基底层只保留共享文件与共享规则
3. 不在这里增加面向最终用户的模板叙事
