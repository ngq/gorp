# 共享基底结构

`project` 只提供三类公开模板都会复用的共享片段。

## 当前保留

- `.gorp-template.yml`
- `.gitignore`
- `README.md`
- `docs/`
- `deploy/kubernetes/`

## 当前不保留

- `cmd/app`
- `app/http`
- `internal/server`
- `internal/biz`
- `internal/data`
- `internal/service`
- `config/app.yaml`
- `go.mod`
- Dockerfile
- Makefile
- CI workflow

## 边界规则

单服务入口属于 `golayout`。

微服务入口、Wire 收口、`internal/server/http`、`internal/server/grpc` 属于 `multi-flat-wire` 或 `multi-independent`。

`project` 不解释业务开发方式，只提供可复用的根层文档与交付片段。
