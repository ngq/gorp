# 下一步怎么开始业务开发

当你已经可以访问：

- `GET /healthz`
- `GET /api/ping`

说明这个最小骨架已经跑通。接下来建议按下面顺序继续。

> 定位提醒：
> - `project` 是共享骨架基底，不是公开默认 starter；
> - 如果你需要默认单服务生产起步体验，优先使用 `gorp new` 生成的默认 starter，而不是继续把 `project` 当成公开模板。

## 1. 先看 HTTP 入口

优先阅读：

- `app/http/routes.go`
- `app/http/handler/health.go`
- `app/http/handler/ping.go`

这样你能快速理解：
- 路由挂载在哪
- handler 放在哪
- service 怎么被调用

## 2. 再新增你自己的第一个接口

建议先模仿 `Ping` 这条链路：

1. 在 `app/service/` 增加一个业务函数
2. 在 `app/http/handler/` 增加一个 handler
3. 在 `app/http/routes.go` 里挂路由

## 3. DTO 放在哪

- 请求结构：`app/http/request/`
- 响应结构：`app/http/response/`

## 4. 中间件放在哪

如果后续需要鉴权、日志、限流等项目级中间件，可以放到：

- `app/http/middleware/`

## 5. 如果你准备接数据库

数据库相关能力按需再看工具链命令，不把它放进默认首屏路径。

## 6. 如果你准备接 gRPC

当前 gRPC 正式主线已经收口为 Proto-first，建议按下面心智接入：

1. 定义 `.proto`
2. 生成 `pb.go / *_grpc.pb.go`
3. 服务端通过 `GRPCServerRegistrar` 挂 `pb.RegisterXxxServer(...)`
4. 客户端通过 `GRPCConnFactory` 获取连接，再用 `pb.NewXxxClient(conn)` 发起调用

建议理解：

- 不再把字符串式 `RPCClient.Call(...)` 作为 gRPC 主路径；
- `gorp grpc` 命令组当前仍属于 legacy/runtime 兼容链路，不是 starter 默认公开入口；
- 正式主线应围绕标准 gRPC stub / register 工作。

## 7. 如果你准备做成可发布项目

固定版本交付相关动作放到真正需要发布时再做：

```bash
gorp template pack
gorp new from-release --repo <owner>/<repo> --tag latest
```
