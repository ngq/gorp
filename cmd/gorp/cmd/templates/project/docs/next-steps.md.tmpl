# 下一步

模板重写按下面顺序推进：

1. 先稳定 `project` 共享基底
2. 再重写 `golayout`
3. 再重写 `multi-flat-wire`
4. 最后重写 `multi-independent`

`project` 阶段只处理共享边界，不承诺业务启动入口。
这里出现的共享部署片段、目录骨架与模板入口，只代表公开模板复用边界，不代表任何具体 `contrib` 后端已经进入“已完成”状态。

公开模板重写时需要遵守：

- 默认入口消费 facade 契约，不回退到历史根包入口
- README 首屏只写入口、目录、业务开发主线
- docs 只解释目录边界、生成步骤和部署入口
- 第三方能力只放配置与场景化使用示例，不按组件名堆目录
