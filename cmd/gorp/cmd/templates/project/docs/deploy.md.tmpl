# 共享交付片段

`project` 只保留最小 Kubernetes 片段，作为公开模板重写时可复用的基础。

## 当前包含

- `deploy/kubernetes/base/`
- `deploy/kubernetes/overlays/dev/`
- `deploy/kubernetes/overlays/staging/`

说明：
- 这里提供的是共享 Kubernetes 部署片段与目录骨架，便于公开模板复用。
- 这不代表 `contrib/configsource/kubernetes` 或 `contrib/registry/kubernetes` 已经完成真实后端能力接入。

## 当前不包含

- Dockerfile
- Makefile
- CI workflow
- 业务服务启动命令
- 生产发布流程说明

这些内容由 `golayout`、`multi-flat-wire`、`multi-independent` 在各自场景内定义。
