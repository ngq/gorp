# multi-flat-wire 模板目录说明

`multi-flat-wire` 是多服务的高级模板，适合明确希望在业务项目中使用 Wire 做编译期装配的团队。

> 定位提醒：
> - 它是**高级模板**，不是默认推荐模板；
> - 如果你只是要默认多服务起步，优先使用 `multi-flat`；
> - `multi-flat-wire` 更适合已经明确接受 Wire 装配复杂度、希望把服务依赖图收敛在编译期的团队。

## 当前模板目录

```text
├── services/                      # 各服务代码（按服务就近聚合）
│   ├── user/
│   │   ├── cmd/                   # 服务启动入口与 Wire 装配边界
│   │   │   ├── main.go
│   │   │   ├── wire.go
│   │   │   └── wire_gen.go
│   │   ├── internal/
│   │   │   ├── biz/               # 领域规则 / usecase
│   │   │   ├── data/              # 数据访问实现
│   │   │   ├── service/           # 应用服务编排
│   │   │   └── server/            # HTTP / gRPC 传输注册
│   │   └── config/                # 服务本地配置
│   ├── order/
│   └── product/
├── proto/                         # Proto-first 契约目录
├── deployments/                   # 本地联调 / 容器部署资源
├── scripts/                       # 脚本辅助目录
├── pkg/                           # 公共包（仅放稳定、跨服务复用的代码）
└── docs/                          # 模板内结构说明文档
```

## 各层职责

### 1. `services/<service>/cmd/`

职责：

- 服务启动入口
- Wire 装配声明
- 依赖图生成结果收口

这里解决的是：

- 这个服务如何启动
- 这个服务依赖哪些业务对象
- 这些对象如何通过 Wire 在编译期拼起来

不建议把业务逻辑直接写在这里。

### 2. `services/<service>/internal/biz/`

职责：

- 领域规则
- usecase
- 业务核心行为

这里应该尽量只表达业务含义，不直接承接中间件接入细节。

### 3. `services/<service>/internal/data/`

职责：

- 仓储实现
- 数据持久化
- 与数据库或存储层的具体读写对接

如果某类第三方组件本质上是“数据访问型依赖”，例如局部缓存读写，也可以在这里被业务仓储间接使用；但不要把整个第三方基础设施目录直接塞进 `data/`。

### 4. `services/<service>/internal/service/`

职责：

- 应用服务编排
- 调用 biz / data / server 之间的协作逻辑
- 对接 framework capability 的业务使用入口

这里可以出现“发消息”“调 RPC”“查缓存”的业务动作编排，但不应该直接放底层 RabbitMQ / Kafka / Redis SDK 初始化细节。

### 5. `services/<service>/internal/server/`

职责：

- HTTP / gRPC 注册
- Handler / Transport Adapter
- 请求与响应边界处理

这里只做传输层接线，不承载业务核心逻辑，也不承载第三方组件接入代码。

### 6. `proto/`

职责：

- Proto-first 契约
- gRPC 对外接口定义
- 跨服务通信协议边界

微服务正式主线应优先围绕 `proto/` 与标准 gRPC stub/register 展开，而不是回到字符串式 RPC 调用。

---

## 第三方组件放哪里

原则：

- 不建议在项目根目录直接建一个总兜底的 `third_party/`
- 不建议把 RabbitMQ / Redis / Kafka 这类中间件 SDK 直接写进 `internal/service/`
- 应先区分“业务编排”和“基础设施接入细节”

### 推荐理解

#### 业务编排层
放在：

- `services/<service>/internal/service/`

例如：

- 下单后发布订单创建事件
- 支付完成后发送库存扣减消息
- 查询数据前先读缓存

这里表达的是**什么时候用**这些能力。

#### 基础设施接入细节
当前模板阶段：

- 先通过 framework provider / capability 理解
- 先在文档中明确目录规划
- 当前不默认把它们物化成一层模板代码目录

这里表达的是**怎么接中间件**。

---

## 微服务 / 第三方组件的推荐分组

后续如果继续扩展目录说明，建议按职责分组，而不是按供应商分组。

### A. 缓存与键值组件

例如：

- Redis
- framework cache

推荐心智：

- 业务如何使用缓存：放 `internal/service/`
- 缓存能力来源：优先走 framework capability
- 不建议把 Redis SDK 初始化直接分散到业务 service

### B. 消息与事件组件

例如：

- RabbitMQ
- Kafka
- Redis PubSub
- framework message queue

推荐心智：

- 业务何时发消息：放 `internal/service/`
- 消息中间件如何接入：优先保持 framework message queue 心智
- 当前不建议直接建根级 `mq/` 或 `third_party/mq/`

### C. 服务发现与配置源组件

例如：

- discovery
- config source

推荐心智：

- 它们属于运行时治理能力
- 当前优先通过 framework provider 使用
- 不建议在模板中提前铺开一层 project-owned 客户端封装目录

### D. 可观测性组件

例如：

- tracing
- metrics
- error reporting

推荐心智：

- 属于跨服务治理能力
- 不属于单个业务模块领域逻辑
- 当前继续保持 framework capability 入口表达

### E. 韧性治理组件

例如：

- dlock
- circuitbreaker
- retry

推荐心智：

- 它们是运行时稳定性能力
- 应与业务 usecase 分离
- 当前阶段先作为文档规划，不直接物化目录

### F. 外部集成组件

例如：

- HTTP client
- 第三方 API client
- 外部平台接入 adapter

推荐心智：

- 业务调用动作可以出现在 `internal/service/`
- 但外部调用细节不应全部直接堆在 service 中
- 后续若要进一步整理，应按“集成职责”归类，而不是按“第三方来源”归类

---

## 当前阶段为什么只新增 `docs/`

当前模板里只先新增：

- `docs/`

原因是：

- 先给目录树说明预留承接点
- 避免把规划中的结构误写成已经落地的代码目录
- 避免现在过早引入 `internal/infrastructure` / `shared/infrastructure` / `third_party` 等目录，造成模板使用者误解

所以当前阶段的正确理解是：

- 先把规则说清楚
- 再决定后续是否要把部分目录真正物化

---

## 一句话总结

`multi-flat-wire` 当前仍应以“服务目录 + Wire 装配 + framework capability”作为主心智；微服务与第三方组件先按职责做目录规划说明，不要急着直接铺开一层新的基础设施代码目录。