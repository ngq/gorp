# {{.ProjectName}}

基于 gorp 框架的三层架构单服务模板（Wire 装配版）。

> 定位说明：
> - 这是单服务的**高级模板**，适合明确希望采用 Wire 编译期装配的项目；
> - 如果你只是想按默认路径快速开始单服务业务开发，优先使用裸 `gorp new`；
> - `wire` 主要解决装配边界显式化，不代表另一条 starter 产品主线。

## 项目结构

```text
{{.ProjectName}}/
├── cmd/app/
│   ├── main.go            # 应用入口
│   ├── wire.go            # Wire 依赖定义
│   └── wire_gen.go        # Wire 生成结果
├── config/
│   ├── app.yaml
│   └── config.yaml
├── app/
│   └── http/
│       └── routes.go
├── internal/
│   ├── biz/
│   ├── data/
│   └── service/
├── go.mod
├── Makefile
└── Dockerfile
```

## 启动装配策略

- framework 负责提供 config / log / gin / orm / bootstrap 等通用能力；
- 业务依赖图通过 Wire 在 `cmd/app` 层编译期装配；
- 默认模板不把 provider/container 暴露给业务层作为主要工作流。

## 快速开始

```bash
go mod tidy

# 安装 wire（如本机尚未安装）
go install github.com/google/wire/cmd/wire@latest

# 重新生成 wire 代码（可选）
make gen-wire

# 启动项目
go run ./cmd/app
```

## API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /healthz | 健康检查 |
| POST | /api/v1/demos | 创建 Demo |
| GET | /api/v1/demos/:id | 获取 Demo |
| GET | /api/v1/demos | 列表 Demo |
| PUT | /api/v1/demos/:id | 更新 Demo |
| DELETE | /api/v1/demos/:id | 删除 Demo |

## 日志使用建议

- 默认直接使用 framework 提供的 logger 能力；
- 如果需要 trace_id / request_id 等动态字段，优先通过 logger 的上下文增强能力处理；
- 不把从 context 重新获取 logger 作为默认业务写法。

## Makefile 命令

| 命令 | 说明 |
|-----|-----|
| `make build` | 构建应用 |
| `make gen-wire` | 生成 Wire 代码 |
| `make run` | 启动应用 |
| `make test` | 运行测试 |
