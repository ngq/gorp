# {{.ProjectName}}

基于 gorp 框架的三层架构微服务模板。

## 项目结构

```
{{.ProjectName}}/
├── cmd/app/                # 应用入口
│   └── main.go             # gorp CLI 入口
├── config/                 # 配置文件
│   └── app.yaml
├── app/                    # 应用层
│   ├── http/               # HTTP 路由
│   │   └── routes.go
│   └── provider/           # Provider 注册
│       └── service/provider.go
├── internal/               # 内部实现
│   ├── biz/                # 业务逻辑层
│   ├── data/               # 数据访问层
│   └── service/            # 服务层
├── go.mod
├── Makefile
└── Dockerfile
```

## 架构说明

| 层级 | 目录 | 职责 |
|------|------|------|
| 入口 | cmd/app/ | gorp CLI 命令入口 |
| 应用 | app/ | 路由注册、Provider 配置 |
| 业务 | internal/biz/ | 领域对象、仓储接口、用例 |
| 数据 | internal/data/ | 仓储实现、数据库操作 |
| 服务 | internal/service/ | HTTP handler、请求响应转换 |

## 依赖方向

```
service → biz → data
          ↑
      接口定义在 biz，实现在 data
```

## 快速开始

```bash
# 安装依赖
go mod tidy

# 运行（直接通过项目 main 入口启动）
go run ./cmd/app

# 或使用 Makefile
make run
```

## API 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /healthz | 健康检查 |
| POST | /api/v1/demos | 创建 Demo |
| GET | /api/v1/demos/:id | 获取 Demo |
| GET | /api/v1/demos | 列表 Demo |
| PUT | /api/v1/demos/:id | 更新 Demo |
| DELETE | /api/v1/demos/:id | 删除 Demo |

## 配置

配置文件位于 `config/app.yaml`。

## License

MIT