package http

import (
	"{{.FrameworkModule}}/app/http/handler"
	gorp "{{.FrameworkModule}}"
)

// RegisterRoutes 挂载 starter 默认业务路由。
func RegisterRoutes(router gorp.Router) {
	router.GET("/api/ping", handler.Ping)
}
