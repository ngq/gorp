package handler

import (
	ginprovider "{{.FrameworkModule}}/framework/provider/gin"

	"github.com/gin-gonic/gin"
)

// Health 返回 starter 默认健康检查响应。
func Health(c *gin.Context) {
	ginprovider.Success(c, gin.H{"status": "ok"})
}
