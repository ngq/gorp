package handler

import (
	gorp "{{.FrameworkModule}}"
	"{{.FrameworkModule}}/app/service"
	"{{.FrameworkModule}}/framework/httpx"
)

// Ping 返回 starter 默认的最小业务探针响应。
func Ping(c gorp.Context) {
	httpx.Success(c, map[string]any{"message": service.Ping()})
}
