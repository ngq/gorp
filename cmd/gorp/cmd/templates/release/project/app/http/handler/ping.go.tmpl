package handler

import (
	gorp "{{.FrameworkModule}}"
	"{{.FrameworkModule}}/app/service"
)

// Ping 返回 starter 默认的最小业务探针响应。
func Ping(c gorp.HTTPContext) {
	gorp.Success(c, map[string]any{"message": service.Ping()})
}
