# Starter 项目目录说明

当前 starter 默认包含以下关键目录：

- `cmd/app/`：程序入口
- `app/http/`：HTTP 路由入口
- `app/service/`：业务服务层
- `app/model/`：数据模型层（预留）
- `app/provider/`：自定义 provider（预留）
- `config/`：基础配置
- `storage/`：日志与运行时目录

## 推荐扩展顺序

1. 先在 `app/http/routes.go` 中增加新的接口
2. 再把业务逻辑放到 `app/service/`
3. 后续再补 `app/model/` 与 provider
