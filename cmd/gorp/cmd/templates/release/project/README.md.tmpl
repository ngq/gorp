# {{.Name}}

由 `gorp new from-release` 生成的补充交付 starter。

> 定位：
> - 这不是默认推荐的业务起步路径；
> - 默认业务起步优先使用 `gorp new`；
> - 只有明确需要 release 交付物时，再进入这里。

说明：
- 这里的 starter 交付能力只表示模板主链和发布入口已经整理好。
- 这不代表相关 `contrib` 后端已经进入“已完成”状态。

## 当前模板包含什么

这是一套面向发布场景的 starter，默认包含：

- `cmd/app/main.go`：项目入口
- `app/http/routes.go`：最小 HTTP 路由
- `app/service/ping.go`：最小业务 service 示例
- `config/app.yaml`：基础配置
- `app/service/`、`app/model/`、`app/provider/`：业务扩展目录
- `storage/log/`、`storage/runtime/`：运行期目录占位
- `docs/structure.md`：starter 目录说明
- `.gitignore`：常见忽略规则

## Quick start

```bash
go mod tidy
go run ./cmd/app
```

## 启动后先验证什么

推荐先访问：

- `GET /healthz`
- `GET /api/ping`
- `/swagger/index.html`：取决于配置

## 推荐先看哪几个文件

1. `cmd/app/main.go`
2. `app/http/routes.go`
3. `app/service/ping.go`
4. `config/app.yaml`
5. `docs/structure.md`

## 默认治理入口

- `cmd/app/main.go` 默认通过 `gorp.Run(...) + gorp.WithMicroserviceMode()` 启动
- 如果业务明确要保持轻量单体模式，再切到 `gorp.WithMonolithMode()`
- 默认治理按模式隐式提供；如果需要显式关闭或替换，优先在 `config/app.yaml` 的 `governance` 段落中声明
- `governance.disable` 用于关闭默认治理件，`governance.providers.*` 用于替换默认 provider backend

## release starter 的特点

- 不依赖本地框架源码路径
- 适合通过 GitHub Release 模板资产直接创建项目
- 默认模板资产文件名为：`gorp-template.zip`
- starter 可直接运行，不等于具体后端能力已经完整产品化
