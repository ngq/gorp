# {{.ProjectName}}

基于 gorp 框架的三层架构微服务模板。

## 项目结构

```
{{.ProjectName}}/
├── cmd/app/                # 应用入口
│   └── main.go             # 项目自己的默认启动入口
├── config/                 # 配置文件
│   └── app.yaml
├── app/                    # 应用层
│   └── http/               # HTTP 路由
│       └── routes.go
├── internal/               # 内部实现
│   ├── biz/                # 业务逻辑层
│   ├── data/               # 数据访问层
│   └── service/            # 服务层
├── go.mod
├── Makefile
└── Dockerfile
```

## 架构说明

| 层级 | 目录 | 职责 |
|------|------|------|
| 入口 | cmd/app/ | 项目自己的默认启动入口，直接复用 framework/bootstrap |
| 应用 | app/ | 路由注册与传输层接线 |
| 业务 | internal/biz/ | 领域对象、仓储接口、用例 |
| 数据 | internal/data/ | 仓储实现、数据库操作 |
| 服务 | internal/service/ | HTTP handler、请求响应转换 |

## 依赖方向

```
service → biz → data
          ↑
      接口定义在 biz，实现在 data
```

## 快速开始

```bash
# 安装依赖
go mod tidy

# 运行（直接通过项目 main 入口启动）
go run ./cmd/app

# 或使用 Makefile
make run
```

## 默认起步能力

- HTTP 启动骨架（直接 `go run ./cmd/app`）
- `/healthz` 健康检查
- `/metrics` 指标端点
- GORM 数据库起步链路（sqlite）
- Redis / gRPC 等能力接入位

## API 端点

默认生成结果保留最小 Demo CRUD 样板，方便你直接替换成自己的业务接口。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /healthz | 健康检查 |
| POST | /api/v1/demos | Demo 样板：创建 |
| GET | /api/v1/demos/:id | Demo 样板：获取 |
| GET | /api/v1/demos | Demo 样板：列表 |
| PUT | /api/v1/demos/:id | Demo 样板：更新 |
| DELETE | /api/v1/demos/:id | Demo 样板：删除 |

## 日志使用建议

- 默认优先使用 `gorp/log` 作为业务日志入口；
- 启动装配层之外，先按业务需要再逐步补上下文字段。

## 装配建议

- 默认主线直接使用 `cmd/app/main.go` 中的入口骨架；
- 业务主线直接围绕 `data -> biz -> service -> http routes` 组织代码；
- 更强编译期装配按需再切到 Wire 模板。

## 配置

配置文件位于 `config/app.yaml`。

## License

MIT